#include<stdio.h>
#include<string.h>

void main (){
    char str[20];
    char name[30]="Sian Rodrigues";

    strcpy(str,name);
    puts(str);
// command has been succesfully executed 

    getchar();
}