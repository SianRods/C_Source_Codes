#include<stdio.h>
#include<string.h>

void main(){
    char mail[]= "sian rodrigues maliali jonati";
    int k = strlen(mail);
    printf("The length of the string named mail is : %d",k);
/* Important to point to note here that the length of string 
here is excluding the null character */

    getchar();
}